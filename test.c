#include <stdio.h>
//此部分为加载，一个c文件中只能有一个main
int main01(void)
//ctrl s可以保存文件
{//{}内部代表着代码块，printf意思是格式化输出，要记住每一个代码块后面都要加上;，同时return 0代表着程序结束后返回
	printf(" \ntest\n\a\a\a");//这里的\a是响铃
	return 0;
}
//注意下方的错误列表和输出，可以通过双击错误列表中的错误来定位代码错误的位置与原因，拒绝访问式错误大多是由于未使用管理员权限导致的的
//以及可能出现的代码错误，杀软未关闭等，bl锁一定要关闭，还有在代码所在的盘加入安全盘
//可以通过remove来保证文件存在的前提下可以不运行该程序
//sln是公认的项目工程文件